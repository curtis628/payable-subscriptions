"""Application configuration file for venmo-subscriptions."""
from django.apps import AppConfig


class VenmoSubscriptionsConfig(AppConfig):
    """Configuration details for venmo-subscriptions."""

    name = "venmosubs"
    verbose_name = "venmo-subscriptions"
