"""Application configuration file for payable-subscriptions."""
from django.apps import AppConfig


class PayableSubscriptionsConfig(AppConfig):
    """Configuration details for payable-subscriptions."""

    name = "payablesubs"
    verbose_name = "payable-subscriptions"
